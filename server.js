var app = require('./server-config.js');

var port = 4568;

app.listen(port);
console.log('it really really really really really really fucking worked!');
console.log('Server now listening on port ' + port);
